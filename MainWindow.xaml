<Window x:Class="MDE_Monitoring_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MDE Monitor" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3"
        FontFamily="Segoe UI"
        FontSize="14">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Microsoft Defender for Endpoint Monitor"
                   FontSize="22" FontWeight="Bold"
                   Foreground="#0078D4"
                   Margin="0,0,0,10"/>

        <!-- Tabs -->
        <TabControl Grid.Row="1">
            <!-- Overview Tab -->
            <TabItem Header="Overview">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Defender Status" FontSize="18" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Center"/>
                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0" 
                    Background="#0078D4" Foreground="White" Padding="6,2"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                <Run Text="Last Refreshed: "/>
                <Run Text="{Binding LastRefreshed, StringFormat={}{0:MM/dd/yyyy HH:mm:ss}}"/>
                        </TextBlock>
                    </StackPanel>

                    <UniformGrid Columns="2" Rows="1" Margin="0,10,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                        <!-- Product Info Card -->
                        <Border Background="White" BorderBrush="#0078D4" BorderThickness="1" CornerRadius="5" Padding="15" Margin="5" >
                            <StackPanel>
                                <TextBlock Text="Product Info" FontWeight="Bold" FontSize="16" Foreground="#0078D4" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding DefenderStatus.AMProductVersion, StringFormat=Product Version: {0}}" Margin="0,2"/>
                                <TextBlock Text="{Binding DefenderStatus.AMEngineVersion, StringFormat=Engine Version: {0}}" Margin="0,2"/>
                                <TextBlock Text="{Binding DefenderStatus.AMRunningMode, StringFormat=Running Mode: {0}}" Margin="0,2"/>
                            </StackPanel>
                        </Border>

                        <!-- Protection Status Card -->
                        <Border Background="White" BorderBrush="#0078D4" BorderThickness="1" CornerRadius="5" Padding="15" Margin="5">
                            <StackPanel>
                                <TextBlock Text="Protection Status" FontWeight="Bold" FontSize="16" Foreground="#0078D4" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding DefenderStatus.RealTimeProtection, StringFormat=Real-Time Protection: {0}}" Margin="0,2"/>
                                <TextBlock Text="{Binding DefenderStatus.AntivirusSignatureAge, StringFormat=Antivirus Signature Age: {0}}" Margin="0,2"/>
                                <TextBlock Text="{Binding DefenderStatus.AntispywareSignatureAge, StringFormat=Antispyware Signature Age: {0}}" Margin="0,2"/>
                                <TextBlock Text="{Binding DefenderStatus.DeviceControlDefaultEnforcement, StringFormat=Device Control Enforcement: {0}}" Margin="0,2"/>
                                <TextBlock Text="{Binding DefenderStatus.DeviceControlState, StringFormat=Device Control State: {0}}" Margin="0,2"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </StackPanel>
            </TabItem>


            <!-- Logs Tab -->
            <TabItem Header="Logs">
                <StackPanel Margin="5">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,5">
                        <TextBlock Text="Filter: " VerticalAlignment="Center" FontWeight="Bold"/>
                        <ComboBox Width="120"
                                  SelectedValue="{Binding LogFilter, Mode=TwoWay}"
                                  SelectedValuePath="Content"
                                  Margin="5,0,0,0">
                            <ComboBoxItem Content="All"/>
                            <ComboBoxItem Content="Info"/>
                            <ComboBoxItem Content="Warning"/>
                            <ComboBoxItem Content="Error"/>
                            <ComboBoxItem Content="Critical"/>
                        </ComboBox>
                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                            <Run Text="Last Refreshed: "/>
                            <Run Text="{Binding LastRefreshed, StringFormat={}{0:MM/dd/yyyy HH:mm:ss}}"/>
                        </TextBlock>
                    </StackPanel>

                    <DataGrid x:Name="LogsDataGrid"
                              ItemsSource="{Binding LogsView}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              AlternatingRowBackground="#E6F0FA"
                              RowBackground="White"
                              GridLinesVisibility="Horizontal"
                              CanUserResizeRows="True"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              FontFamily="Segoe UI"
                              FontSize="13"
                              RowHeight="28">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <!-- Error row -->
                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                        <Setter Property="Background" Value="LightCoral"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <!-- Critical row -->
                                    <DataTrigger Binding="{Binding Level}" Value="Critical">
                                        <Setter Property="Background" Value="Tomato"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <!-- Warning row -->
                                    <DataTrigger Binding="{Binding Level}" Value="Warning">
                                        <Setter Property="Background" Value="Khaki"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </DataTrigger>
                                    <!-- Info row (optional) -->
                                    <DataTrigger Binding="{Binding Level}" Value="Info">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat={}{0:MM/dd/yyyy HH:mm:ss}}" Width="180"/>
                            <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="120"/>
                            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding Message}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
