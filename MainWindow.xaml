<Window x:Class="MDE_Monitoring_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MDE_Monitoring_App.Controls"
        xmlns:conv="clr-namespace:MDE_Monitoring_App.Converters"
        Title="MDE Monitor" Height="800" Width="1200"
        MinHeight="700" MinWidth="1000"
        Icon="/Resources/microsoft_defender_icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="#F3F3F3"
        FontFamily="Segoe UI"
        FontSize="14">

    <Window.Resources>
        <conv:DescriptorsJoinConverter x:Key="DescriptorsJoinConverter" Separator="; " />
        <conv:AccessMaskConverter x:Key="AccessMaskConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <conv:BooleanInverseConverter x:Key="BooleanInverseConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <!-- CHANGED: Added a third row (Menu, Header, Content) to prevent overlap -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Menu -->
            <RowDefinition Height="Auto"/>   <!-- Header (title + target controls) -->
            <RowDefinition Height="*"/>      <!-- Main content -->
        </Grid.RowDefinitions>

        <!-- Menu now explicitly in row 0 -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Export PDF..." Click="ExportPdf_Click" />
                <MenuItem Header="Compliance Manager..." Click="FileComplianceManager_Click" />
                <Separator />
                <MenuItem Header="E_xit" Click="FileExit_Click" />

            </MenuItem>
            <MenuItem Header="_Advanced Tools">
                <MenuItem Header="_Netsh Trace..." Click="AdvancedNetworking_Click" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_Usage" Click="HelpUsage_Click" />
                <MenuItem Header="_About" Click="HelpAbout_Click" />
            </MenuItem>
        </Menu>

        <!-- Header grid moved to row 1 -->
        <Grid Grid.Row="1" Margin="0,6,0,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Shortened title to avoid crowding -->
<!--            <TextBlock Grid.Row="0"
                       Text="MDE Monitor"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#0078D4"
                       Margin="0,0,0,4"/>  -->

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Target:"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Foreground="#0078D4"
                           Margin="0,0,8,0"/>

                <TextBox Grid.Column="1"
                         Text="{Binding TargetMachine, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"
                         ToolTip="Enter hostname or IP. Use 'localhost' for local machine."
                         Padding="4,2"
                         MaxLength="300"
                         Width="200"
                         TextWrapping="NoWrap"/>

                <CheckBox Grid.Column="2"
                          Content="Use PsExec"
                          IsChecked="{Binding UsePsExec, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          Margin="12,0,0,0"
                          ToolTip="Use PsExec for remote WFP enumeration (unchecked = WinRM Invoke-Command)"/>

                <TextBox Grid.Column="3"
                         Text="{Binding PsExecPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"
                         Margin="12,0,0,0"
                         Padding="4,2"
                         ToolTip="Path to PsExec.exe (e.g. C:\Sysinternals\PsExec.exe). Must exist if Use PsExec is checked."/>

                <StackPanel Grid.Column="4"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="16,0,0,0">
                    <Button Content="Connect / Refresh"
                            Padding="10,4"
                            Click="RefreshButton_Click"
                            Background="#0078D4"
                            Foreground="White"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource BooleanInverseConverter}, FallbackValue=True}"
                            ToolTip="Fetch data from the specified machine"/>
                    <Grid Width="28" Height="28" Margin="10,0,0,0"
                          Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Canvas Width="28" Height="28">
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="22" Canvas.Top="13" Opacity="1"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="21" Canvas.Top="19" Opacity="0.85"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="14" Canvas.Top="22" Opacity="0.7"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="7"  Canvas.Top="19" Opacity="0.55"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="6"  Canvas.Top="13" Opacity="0.4"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="7"  Canvas.Top="7"  Opacity="0.55"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="14" Canvas.Top="4"  Opacity="0.7"/>
                            <Ellipse Width="5" Height="5" Fill="#0078D4" Canvas.Left="21" Canvas.Top="7"  Opacity="0.85"/>
                            <Canvas.RenderTransform>
                                <RotateTransform Angle="0" CenterX="14" CenterY="14"/>
                            </Canvas.RenderTransform>
                            <Canvas.Triggers>
                                <EventTrigger RoutedEvent="Canvas.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                             From="0" To="360" Duration="0:0:1.0"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Canvas.Triggers>
                        </Canvas>
                    </Grid>
                </StackPanel>

                <TextBlock Grid.Column="5"
                           Margin="18,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="Gray"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap"
                           ToolTip="{Binding TargetMachineStatus}"
                           Text="{Binding TargetMachineStatus}"/>
            </Grid>
        </Grid>

        <!-- Main tab content now in row 2 -->
        <TabControl Grid.Row="2">
            <!-- (UNCHANGED existing TabItems) -->
            <!-- Overview Tab -->
            <TabItem Header="Overview">
                <!-- existing content unchanged -->
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Defender Status" FontSize="18" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Center"/>
                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0" 
                                Background="#0078D4" Foreground="White" Padding="6,2"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                            <!-- "<Run Text="Last Refreshed: "/> -->
                            <Run Text="{Binding LastRefreshed, Mode=OneWay, StringFormat='Last Refreshed: {0:G}'}"/>
                        </TextBlock>
                        <Button Content="Export PDF" Click="ExportPdf_Click" Margin="10,0,0,0"
                                Background="#004578" Foreground="White" Padding="6,2"/>
                    </StackPanel>

                    <!-- Info and Status Grid -->
                    <Grid Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Product Info -->
                        <Border Grid.Row="0" Grid.Column="0"
                                Background="White" BorderBrush="#0078D4" BorderThickness="1"
                                CornerRadius="5" Padding="15" Margin="5">
                            <StackPanel>
                                <TextBlock Text="Product Info" FontWeight="Bold" FontSize="16" Foreground="#0078D4" Margin="0,0,0,10"/>

                                <TextBlock Margin="0,2,0,0">
                                    <Run Text="Platform Version: "/>
                                    <Run Text="{Binding PlatformStatusText, Mode=OneWay}">
                                        <Run.Style>
                                            <Style TargetType="Run">
                                                <Setter Property="Foreground" Value="Gray"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PlatformUpToDate}" Value="True">
                                                        <Setter Property="Foreground" Value="#1E7D24"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding PlatformUpToDate}" Value="False">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Run.Style>
                                    </Run>
                                </TextBlock>

                                <TextBlock Margin="0,2,0,0">
                                    <Run Text="Engine Version: "/>
                                    <Run Text="{Binding EngineStatusText, Mode=OneWay}">
                                        <Run.Style>
                                            <Style TargetType="Run">
                                                <Setter Property="Foreground" Value="Gray"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EngineUpToDate}" Value="True">
                                                        <Setter Property="Foreground" Value="#1E7D24"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EngineUpToDate}" Value="False">
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Run.Style>
                                    </Run>
                                </TextBlock>
                                <TextBlock Text="{Binding DefenderStatus.AntivirusSignatureAge, StringFormat=Antivirus Signature Age: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding DefenderStatus.AntispywareSignatureAge, StringFormat=Antispyware Signature Age: {0}}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Protection Status -->
                        <Border Grid.Row="0" Grid.Column="1"
                                Background="White" BorderBrush="#0078D4" BorderThickness="1"
                                CornerRadius="5" Padding="15" Margin="5">
                            <StackPanel>
                                <TextBlock Text="Protection Status" FontWeight="Bold" FontSize="16" Foreground="#0078D4" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding DefenderStatus.RealTimeProtection, StringFormat=Real-Time Protection: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding DefenderStatus.AMRunningMode, StringFormat=Running Mode: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding DefenderStatus.DeviceControlDefaultEnforcementDisplay, StringFormat=Device Control Enforcement: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding DefenderStatus.DeviceControlState, StringFormat=Device Control State: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Margin="0,2,0,0" Text="{Binding DefenderStatus.TamperProtectionDisplay}"/>
                                <TextBlock Text="{Binding CodeIntegrityPolicyDisplay}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding VbsStatusDisplay}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding SecurityServicesConfiguredDisplay}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding SecurityServicesRunningDisplay}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding FirewallProfilesMultilineDisplay}"
           Margin="0,2,0,0"
           FontFamily="Consolas"
           TextWrapping="Wrap"
           xml:space="preserve"/>
                            </StackPanel>
                        </Border>

                        <!-- System Info -->
                        <Border Grid.Row="1" Grid.Column="0"
                                Background="White" BorderBrush="#0078D4" BorderThickness="1"
                                CornerRadius="5" Padding="15" Margin="5">
                            <StackPanel>
                                <TextBlock Text="System Info" FontWeight="Bold" FontSize="16" Foreground="#0078D4" Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding CurrentSystem.CurrentUser, StringFormat=Current User: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding CurrentSystem.MachineName, StringFormat=Machine Name: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding CurrentSystem.IPAddress, StringFormat=IP Address: {0}}" Margin="0,2,0,0"/>
                                <TextBlock Text="{Binding CurrentSystem.JoinType, StringFormat=Join Type: {0}}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Intune / Entra Management -->
                        <Border Grid.Row="1" Grid.Column="1"
        Background="White" BorderBrush="#0078D4" BorderThickness="1"
        CornerRadius="5" Padding="15" Margin="5">
                            <StackPanel>
                                <TextBlock Text="Intune / Entra Management" FontWeight="Bold" FontSize="16" Foreground="#0078D4" Margin="0,0,0,10"/>

                                <TextBlock Margin="0,2,0,0">
            <Run Text="Last Sync (Local): "/>
            <Run Text="{Binding IntuneLastSyncLocalDisplay, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Margin="0,2,0,0">
            <Run Text="Last Sync (UTC): "/>
            <Run Text="{Binding IntuneLastSyncUtcDisplay, Mode=OneWay}"/>
                                </TextBlock>

                                <Separator Margin="0,10,0,6"/>

                                <TextBlock Margin="0,2,0,0">
            <Run Text="Enrollment Type: "/>
            <Run Text="{Binding IntuneEnrollmentEffectiveType, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Margin="0,2,0,0">
            <Run Text="Origin: "/>
            <Run Text="{Binding IntuneEnrollmentOrigin, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Margin="0,2,0,0">
            <Run Text="Enrollment State: "/>
            <Run Text="{Binding IntuneEnrollmentState, Mode=OneWay}"/>
                                </TextBlock>
                                <TextBlock Margin="0,2,0,0" TextWrapping="Wrap">
            <Run Text="UPN: "/>
            <Run Text="{Binding IntuneEnrollmentUpn, Mode=OneWay}"/>
                                </TextBlock>

                                <!-- Co-managed badge -->
                                <TextBlock Margin="0,6,0,0"
                                       Padding="6,2"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Text="Co-managed"
                                       Background="#FFE59D"
                                       Foreground="#5D3B00">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsIntuneCoManaged}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- Combined line -->
                                <TextBlock Margin="0,10,0,0"
               FontStyle="Italic"
               Foreground="Gray"
               TextWrapping="Wrap"
               Text="{Binding IntuneEnrollmentDisplay}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </TabItem>

            <!-- Device Control tab -->
            <TabItem Header="Device Control">
                <!-- unchanged -->
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Device Control Logs"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#0078D4"
                               Margin="0,0,0,10"
                               Grid.Row="0"/>
                    <DataGrid ItemsSource="{Binding DeviceControlEvents}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              AlternatingRowBackground="#E6F0FA"
                              RowBackground="White"
                              FontSize="13"
                              RowHeight="28"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto"
                              Grid.Row="1">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DeniedAccess}" Value="None">
                                        <Setter Property="Background" Value="#DFF6DD"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="FontWeight" Value="Normal"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp}" Width="150"/>
                            <DataGridTextColumn Header="InstancePathId" Binding="{Binding InstancePathId}" Width="*"/>
                            <DataGridTextColumn Header="VID" Binding="{Binding VID}" Width="100"/>
                            <DataGridTextColumn Header="PID" Binding="{Binding PID}" Width="100"/>
                            <DataGridTextColumn Header="Granted" Binding="{Binding GrantedAccess}" Width="150"/>
                            <DataGridTextColumn Header="Denied" Binding="{Binding DeniedAccess}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Device Control Policies Tab -->
            <TabItem Header="Device Control Policies">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Device Control Policies"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#0078D4"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding DeviceControlPolicyStatus}"
                                   Margin="20,0,0,0"
                                   VerticalAlignment="Center"
                                   Foreground="Gray"/>
                        <Button Content="Refresh"
                                Margin="20,0,0,0"
                                Padding="6,2"
                                Background="#0078D4"
                                Foreground="White"
                                Click="RefreshButton_Click"/>
                    </StackPanel>

                    <!-- Policy Rules -->
                    <!-- REPLACE ONLY the Policy Rules DataGrid opening tag and closing tag block with this updated version -->
                    <GroupBox Header="Policy Rules" Grid.Row="1" Margin="0,0,0,10">
                        <DataGrid x:Name="PolicyRulesGrid"
              ItemsSource="{Binding DeviceControlPolicyRules}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              AlternatingRowBackground="#E6F0FA"
              RowBackground="White"
              FontSize="13"
              RowHeight="26"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              ScrollViewer.HorizontalScrollBarVisibility="Auto"
              RowDetailsVisibilityMode="Collapsed"    
                            SelectionMode="Single"
              PreviewMouseLeftButtonDown="PolicyRulesGrid_PreviewMouseLeftButtonDown">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                                <DataGridTextColumn Header="Rule Id" Binding="{Binding Id}" Width="210"/>
                                <DataGridTextColumn Header="Included Groups" Binding="{Binding IncludedGroupsDisplay}" Width="*"/>
                                <DataGridTextColumn Header="Excluded Groups" Binding="{Binding ExcludedGroupsDisplay}" Width="*"/>
                                <DataGridTextColumn Header="Entry Summary" Binding="{Binding EntrySummary}" Width="180"/>
                            </DataGrid.Columns>

                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <Border Margin="4" Padding="4" Background="#F9FBFD" BorderBrush="#C8D7E3" BorderThickness="1" CornerRadius="4">
                                        <DataGrid ItemsSource="{Binding Entries}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              RowHeight="24"
                              FontSize="12"
                              GridLinesVisibility="Horizontal"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                                            <DataGrid.Columns>
                                                <DataGridTextColumn Header="Entry Id" Binding="{Binding Id}" Width="200"/>
                                                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="120"/>
                                                <DataGridTextColumn Header="Options" Binding="{Binding Options}" Width="80"/>
                                                <DataGridTextColumn Header="AccessMask"
                                                Binding="{Binding AccessMask, Converter={StaticResource AccessMaskConverter}}"
                                                Width="220"/>
                                                <DataGridTextColumn Header="Sid" Binding="{Binding Sid}" Width="240"/>
                                                <DataGridTextColumn Header="ComputerSid" Binding="{Binding ComputerSidDisplay}" Width="260"/>
                                            </DataGrid.Columns>
                                        </DataGrid>
                                    </Border>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>

                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="ToolTip" Value="Click to expand / collapse entries"/>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </GroupBox>

                    <!-- Policy Groups -->
                    <GroupBox Header="Policy Groups" Grid.Row="2">
                        <DataGrid ItemsSource="{Binding DeviceControlPolicyGroups}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  AlternatingRowBackground="#E6F0FA"
                                  RowBackground="White"
                                  FontSize="13"
                                  RowHeight="26"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Group Id" Binding="{Binding Id}" Width="300"/>
                                <DataGridTextColumn Header="Match Type" Binding="{Binding MatchType}" Width="140"/>
                                <DataGridTextColumn Header="Descriptors"
                                                    Width="*"
                                                    Binding="{Binding Descriptors, Converter={StaticResource DescriptorsJoinConverter}}">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Descriptors, Converter={StaticResource DescriptorsJoinConverter}}"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Firewall Tab -->
            <TabItem Header="Firewall">
                <!-- unchanged -->
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Firewall Drops" FontSize="18" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Center"/>
                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0"
                                Background="#0078D4" Foreground="White" Padding="6,2"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                            <Run Text="Last Refreshed: "/>
                            <Run Text="{Binding LastRefreshed, Mode=OneWay, StringFormat='Last refreshed: {0:G}'}"/>
                        </TextBlock>

                        <!-- Status message for firewall logging -->
                        <TextBlock Margin="10,0,0,0"
                                   Text="{Binding FirewallLoggingStatusMessage}"
                                   Foreground="OrangeRed"
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ShowFirewallLoggingStatusMessage}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <DockPanel Margin="30,0,0,0" VerticalAlignment="Center">
                            <TextBox x:Name="FirewallFilterBox"
                                     Width="220"
                                     VerticalAlignment="Center"
                                     Text="{Binding FirewallFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Type to filter (matches any column)"/>
                            <Button Content="×"
                                    Width="22"
                                    Height="22"
                                    Margin="4,0,0,0"
                                    VerticalAlignment="Center"
                                    Padding="0"
                                    ToolTip="Clear filter"
                                    Click="ClearFirewallFilter_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="IsEnabled" Value="True"/>
                                        <Setter Property="Opacity" Value="1"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding FirewallFilterText}" Value="">
                                                <Setter Property="IsEnabled" Value="False"/>
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DockPanel>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding FirewallView}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              AlternatingRowBackground="#E6F0FA"
                              RowBackground="White"
                              FontSize="13"
                              RowHeight="26"
                              Grid.Row="1"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Visible"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170"/>
                            <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="80"/>
                            <DataGridTextColumn Header="Proto" Binding="{Binding Protocol}" Width="70"/>
                            <DataGridTextColumn Header="Src IP" Binding="{Binding SourceIp}" Width="140"/>
                            <DataGridTextColumn Header="Src Port" Binding="{Binding SourcePort}" Width="80"/>
                            <DataGridTextColumn Header="Dst IP" Binding="{Binding DestinationIp}" Width="140"/>
                            <DataGridTextColumn Header="Dst Port" Binding="{Binding DestinationPort}" Width="80"/>
                            <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="70"/>
                            <DataGridTextColumn Header="Info" Binding="{Binding Info}" Width="100"/>
                            <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70"/>
                            <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="300">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding Path}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- WFP Filters Tab -->
            <TabItem Header="WFP Filters">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
                        <TextBlock Text="WFP Filter Summary"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#0078D4"
                           VerticalAlignment="Center"/>
                        <TextBlock Margin="20,0,0,0"
                           VerticalAlignment="Center"
                           ToolTip="{Binding WfpFilterCountTooltip}">
                            <TextBlock.Text>
                                <Binding Path="WfpFilterCount"
                                 TargetNullValue="Total: Unknown"
                                 StringFormat="Total: {0:N0}"/>
                            </TextBlock.Text>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WfpFilterCountStatus}" Value="Unknown">
                                            <Setter Property="Foreground" Value="Gray"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding WfpFilterCountStatus}" Value="Large">
                                            <Setter Property="Foreground" Value="Goldenrod"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding WfpFilterCountStatus}" Value="High">
                                            <Setter Property="Foreground" Value="Red"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Button Content="Refresh"
                        Click="RefreshButton_Click"
                        Margin="20,0,0,0"
                        Background="#0078D4"
                        Foreground="White"
                        Padding="6,2"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1"
                      ItemsSource="{Binding WfpRuleCounts}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      AlternatingRowBackground="#E6F0FA"
                      RowBackground="White"
                      FontSize="13"
                      RowHeight="26"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Rule Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- App Control Tab -->
            <TabItem Header="App Control">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="App Control (Code Integrity / AppLocker)" FontSize="18" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Center"/>
                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0"
                    Background="#0078D4" Foreground="White" Padding="6,2"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                <Run Text="Last Refreshed: "/>
                <Run Text="{Binding LastRefreshed, Mode=OneWay, StringFormat='Last refreshed: {0:G}'}"/>
                        </TextBlock>
                    </StackPanel>

                    <DataGrid x:Name="AppControlDataGrid"
          Grid.Row="1"
          ItemsSource="{Binding AppControlEvents}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          AlternatingRowBackground="#E6F0FA"
          RowBackground="White"
          FontSize="13"
          RowHeight="26"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.HorizontalScrollBarVisibility="Auto"
          RowDetailsVisibilityMode="Collapsed"
          PreviewMouseLeftButtonDown="AppControlDataGrid_PreviewMouseLeftButtonDown">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                            <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80"/>
                            <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="220"/>
                            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="NoWrap"/>
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding Message}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>

                        <DataGrid.RowDetailsTemplate>
                            <DataTemplate>
                                <Border Margin="4"
                    Padding="6"
                    Background="#FAFCFE"
                    BorderBrush="#D0DDE8"
                    BorderThickness="1"
                    CornerRadius="4">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Time, StringFormat=Time: {0:yyyy-MM-dd HH:mm:ss}}"
                               FontWeight="Bold" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding Level, StringFormat=Level: {0}}"
                               Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding Channel, StringFormat=Channel: {0}}"
                               Margin="0,0,0,4"/>
                                        <TextBlock Text="Message:" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Message}"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </DataGrid.RowDetailsTemplate>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="ToolTip" Value="Click to expand / collapse event details"/>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Policies Tab -->
            <TabItem Header="Policies">
                <!-- unchanged -->
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Defender Policies" FontSize="18" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Center"/>
                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0"
                                Background="#0078D4" Foreground="White" Padding="6,2"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                            <Run Text="Last Refreshed: "/>
                            <Run Text="{Binding LastRefreshed, Mode=OneWay, StringFormat='Last refreshed: {0:G}'}"/>
                        </TextBlock>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding PolicyView}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              AlternatingRowBackground="#E6F0FA"
                              RowBackground="White"
                              FontSize="13"
                              Grid.Row="1"
                              RowHeight="28"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Severity}" Value="Risk">
                                        <Setter Property="Background" Value="MistyRose"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Severity}" Value="Error">
                                        <Setter Property="Background" Value="LightCoral"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Friendly Name" Binding="{Binding DisplayName}" Width="200"/>
                            <DataGridTextColumn Header="Interpreted" Binding="{Binding InterpretedValue}" Width="130"/>
                            <DataGridTextColumn Header="Raw" Binding="{Binding RawValue}" Width="80"/>
                            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="ToolTip" Value="{Binding Description}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Compliance Tab (NEW) -->
            <TabItem Header="Compliance">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header / Pie -->
                    <DockPanel Grid.Row="0" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                            <StackPanel Margin="0,0,18,0">
                                <TextBlock Text="Compliance Overview"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="#0078D4"/>
                                <!-- NEW: Prominent current policy name 
                                <TextBlock Text="{Binding CompliancePolicyName}"
                                           Margin="0,4,0,0"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="#004578"
                                           ToolTip="Current compliance policy name"/>-->
                            </StackPanel>

                            <controls:CompliancePieChart Percentage="{Binding CompliancePercentage}"
                                                         Width="130" Height="130"
                                                         ToolTip="{Binding ComplianceSummaryDisplay}"/>

                            <StackPanel Margin="20,0,0,0" VerticalAlignment="Center" Width="320">
                                <!-- UPDATED: Use existing properties from MainViewModel -->
                                <TextBlock Text="{Binding CompliancePolicyDescription}"
                                           TextWrapping="Wrap"
                                           Foreground="Gray"
                                           FontSize="12"/>
                                <TextBlock Text="{Binding ComplianceSummaryDisplay}"
                                           FontWeight="Bold"
                                           Margin="0,8,0,0"
                                           Foreground="#1E7D24"/>
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Reload Policy"
                                            Width="120"
                                            Padding="6,2"
                                            Click="ReloadCompliancePolicy_Click"
                                            ToolTip="Reload CompliancePolicy.config.json"/>
                                    <!-- OPTIONAL: Show file name if not (none) -->
                                    <TextBlock Margin="12,4,0,0"
                                               Foreground="Gray"
                                               FontSize="11"
                                               Text="{Binding CompliancePolicyName, StringFormat=Policy: {0}}"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </DockPanel>

                    <!-- Detailed settings list -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding ComplianceItems}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              AlternatingRowBackground="#E6F0FA"
                              RowBackground="White"
                              FontSize="13"
                              RowHeight="26"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Setting"   Binding="{Binding Name}"            Width="220"/>
                            <DataGridTextColumn Header="Required"  Binding="{Binding RequiredDisplay}" Width="110"/>
                            <DataGridTextColumn Header="Current"   Binding="{Binding CurrentDisplay}"  Width="120"/>
                            <DataGridTextColumn Header="Compliant" Binding="{Binding CompliantDisplay}" Width="90"/>
                            <DataGridTextColumn Header="Notes"     Binding="{Binding Notes}"          Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="ToolTip" Value="{Binding Notes}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Compliant}" Value="False">
                                        <Setter Property="Background" Value="#FFF4D4"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Compliant}" Value="True">
                                        <Setter Property="Background" Value="#DFF6DD"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Logs Tab -->
            <TabItem Header="Logs">
                <!-- unchanged -->
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10" Grid.Row="0">
                        <TextBlock Text="Filter: " VerticalAlignment="Center" FontWeight="Bold" Foreground="#0078D4"/>
                        <ComboBox Width="120"
                                  SelectedValue="{Binding LogFilter, Mode=TwoWay}"
                                  SelectedValuePath="Content"
                                  Margin="5,0,0,0">
                            <ComboBoxItem Content="All"/>
                            <ComboBoxItem Content="Info"/>
                            <ComboBoxItem Content="Warning"/>
                            <ComboBoxItem Content="Error"/>
                            <ComboBoxItem Content="Critical"/>
                        </ComboBox>

                        <!-- NEW: Time Range Selector -->
                        <TextBlock Text="Range:" Margin="20,0,0,0" VerticalAlignment="Center" FontWeight="Bold" Foreground="#0078D4"/>
                        <ComboBox Width="120"
                                  Margin="5,0,0,0"
                                  SelectedValuePath="Tag"
                                  SelectedValue="{Binding LogTimeRangeHours, Mode=TwoWay}">
                            <ComboBoxItem Content="Last 1 hr"  Tag="1"/>
                            <ComboBoxItem Content="Last 8 hrs" Tag="8"/>
                            <ComboBoxItem Content="Last 24 hrs" Tag="24"/>
                            <ComboBoxItem Content="Last 48 hrs" Tag="48"/>
                            <ComboBoxItem Content="Last Week" Tag="168"/>
                            <ComboBoxItem Content="Last Month" Tag="720"/>
                        </ComboBox>

                        <Button Content="Refresh" Click="RefreshButton_Click" Margin="20,0,0,0"
                                Background="#0078D4" Foreground="White" Padding="6,2"/>
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center">
                            <Run Text="Last Refreshed: "/>
                            <Run Text="{Binding LastRefreshed, Mode=OneWay, StringFormat='Last refreshed: {0:G}'}"/>
                        </TextBlock>
                    </StackPanel>
                    <Border Background="White" BorderBrush="#0078D4" BorderThickness="1" CornerRadius="5" Padding="5" Grid.Row="1">
                        <DataGrid x:Name="LogsDataGrid"
                                  ItemsSource="{Binding LogsView}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  AlternatingRowBackground="#E6F0FA"
                                  RowBackground="White"
                                  GridLinesVisibility="Horizontal"
                                  CanUserResizeRows="True"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                  FontFamily="Segoe UI"
                                  FontSize="13"
                                  RowHeight="28"
                                  RowDetailsVisibilityMode="Collapsed"
                                  PreviewMouseLeftButtonDown="LogsDataGrid_PreviewMouseLeftButtonDown">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                            <Setter Property="Background" Value="LightCoral"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Critical">
                                            <Setter Property="Background" Value="Tomato"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                            <Setter Property="Background" Value="Khaki"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Info">
                                            <Setter Property="Background" Value="White"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat={}{0:MM/dd/yyyy HH:mm:ss}}" Width="180"/>
                                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="120"/>
                                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            <Setter Property="ToolTip" Value="{Binding Message}"/>
                                            <Setter Property="TextWrapping" Value="NoWrap"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>

                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <Border Margin="4"
                    Padding="6"
                    Background="#FAFCFE"
                    BorderBrush="#D0DDE8"
                    BorderThickness="1"
                    CornerRadius="4">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Time, StringFormat=Time: {0:MM/dd/yyyy HH:mm:ss}}"
                               FontWeight="Bold" Margin="0,0,0,2"/>
                                            <TextBlock Text="{Binding Level, StringFormat=Level: {0}}"
                               Margin="0,0,0,4"/>
                                            <TextBlock Text="Message:" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Message}"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>

                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="ToolTip" Value="Click to expand / collapse full log entry"/>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
